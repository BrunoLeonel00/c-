#include <iostream>

int main (){
    int paes = 10;
    // operador de soma +
        //paes = paes + 1;
        //paes += 1;
        //paes++;

    // operador de subtração -
        //paes = paes - 1;
        //paes -= 1;
        //paes--;

    // operador de multiplicação *
        //paes = paes * 2;
        //paes *= 2;
        
    // operador de divisao /
        //paes = paes / 2;
        //paes /= 2;
    
    // operador de resto da divisao %    muito util para saber se o numero é impar ou par
        // paes = paes % 2;
         //paes %= 2;

    std::cout << paes;


    return 0;
}